fade = false;
stuck = false;

image_speed = 0;
rotspeed = (14 + random(18)) * choose(1, -1) * global.delta_factor;
angle = random(360);
speed = 2 + random(3);

alarm[0] = 45 / global.delta_factor;

