with (StabAnim) {
    if (ownerPlayer == other.ownerPlayer) {
        instance_destroy();
    }
}
            
with (StabMask) {
    if (ownerPlayer == other.ownerPlayer) {
        instance_destroy();
    }
}

