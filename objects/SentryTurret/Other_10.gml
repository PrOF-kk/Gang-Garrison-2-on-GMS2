if (startDirection > 0) {
    direction=0;
} else {
    direction=180;
}

if(team == TEAM_RED) {
    rotateoffset=0;
} else {
    rotateoffset=5;
}

rotatestart = rotateoffset;
rotateend = rotateoffset+4;
rotateindex = rotatestart;

